# Borg backup

### Abans de res ...

sudo apt update -y && sudo apt install git  
git clone https://github.com/capbussat/isard-scripts  
cd isard-scripts  
cd "client-borg"  
sudo chmod +x client-borg.sh  
./client-borg.sh  
